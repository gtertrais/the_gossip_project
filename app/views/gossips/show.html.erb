
      
      <div class="jumbotron">
          <h1 class="display-3"><%=@gossip.title %> </h1>
          <p class="lead"> <%= @gossip.content %> </p>
          <hr class="my-4">
          <p><%= User.find(@gossip.user_id).first_name %>
            <%= User.find(@gossip.user_id).last_name %> </p>
          <p class="lead">
              <%= link_to "Link to profile", "../profiles/#{@gossip.user_id}" %>
      
          </p>
        </div>
      



  
          